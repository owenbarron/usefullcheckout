# Version History

## 0.3.3 — With Sound
- Container scan sound on each successful scan
- Final success chime when checkout completes (after ID/card scan)
- Error sound on campus account or frozen account errors
- Sound on/off toggle in settings panel
- Version badge in bottom-left corner of kiosk UI

## 0.3 — With First Kiosk Render
- First full kiosk hardware render (USEFULL checkout station illustration)
- V4 scan layout with two-panel design (scan panel + checkout panel)
- Portrait and landscape orientation presets for Lenovo Legion Tab 3
- PWA / fullscreen kiosk shell with service worker

## 0.2 — Two Buttons
- Two-button variant: separate "Scan Container" and "Tap ID / Tap Card" action buttons
- Refactored core engine (core.js) shared across variants
- Redesigned success screen with container list and app download modal
- Credit card flow with OTP verification
- Settings panel with simulation, debug jump-to, and display controls

## 0.1 — Green Bars
- Initial prototype (green-bars.html)
- Basic scan flow with green progress bar indicators
- Foundation state machine and branding profiles
